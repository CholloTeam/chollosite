from .base import *

DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'samuel',
        'USER': 'samuel',
        'PASSWORD': 'Samuel101',
    }
}

